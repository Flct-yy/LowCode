# .yarnrc.yml 完整配置
nodeLinker: node-modules
# 强制 Yarn 解析 workspace:* 时优先查找本地工作区（关键）
nmMode: hardlinks-global
# 禁用 npm 仓库解析本地 workspace 包（核心修复）
disableImplicitWorkspaceDependencies: false
# 确保工作区包解析优先级高于 npm 仓库
workspaceDependenciesMeta:
  "@wect/*":
    injected: true